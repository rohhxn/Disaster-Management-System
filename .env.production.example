# Production Environment Variables for DAMS
# Copy this file to .env and update with your secure values

NODE_ENV=production

# Database Configuration
DB_USER=dams-admin
DB_PASS=CHANGE_THIS_TO_SECURE_PASSWORD
DB_NAME=dams
DB_HOST=dams-postgres
DB_PORT=5432
DB_DIALECT=postgres

# JWT Token for authentication
JWT_TOKEN=CHANGE_THIS_TO_SECURE_JWT_TOKEN

# Client Configuration
CLIENT_HOST=0.0.0.0
CLIENT_PORT=5050

# Server Configuration
SERVER_HOST=0.0.0.0
SERVER_PORT=5000

# IMPORTANT SECURITY NOTES:
# 1. Generate a strong DB_PASS: openssl rand -base64 32
# 2. Generate a strong JWT_TOKEN: openssl rand -base64 64
# 3. Never commit the actual .env file with real secrets to git
# 4. Keep this file secure with proper permissions: chmod 600 .env
